for numero in range(1, 101):
    if numero % 2 != 0:
        print(numero,end=" ") 
#for inia uma sequencia o range de 1 a 100
#if numero % 2 != 0:
#-Este é um teste condicional que verifica se o número atual (numero) é ímpar.
#-A operação numero % 2 calcula o resto da divisão de numero por 2.
#-Se o resto não for igual a 0 (!= 0), isso significa que o número é ímpar. Caso contrário, o número é par.
#print(numero, end=" "):
#-Se a condição numero % 2 != 0 for verdadeira (ou seja, se o número for ímpar), o número é impresso.
#-print(numero, end=" ") imprime o número seguido de um espaço (" ") em vez de uma nova linha. Isso faz com que todos os números ímpares sejam impressos na mesma linha, separados por um espaço.